@page "/schedule"
@inject Services.ScheduleService ScheduleService

<InputDay OnClickCallback="@GetScheduleDay"/>
<Lessons Day=@Day/>

@code {

    private TimeTableDay Day;

    private void GetScheduleDay(string selectedDay)
    {
        Day = ScheduleService.GetScheduleDay(selectedDay);

        if (Day == null)
            Day = new TimeTableDay
            {
                DayName = selectedDay
            };
    }
}
